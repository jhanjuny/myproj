# 프로젝트 규칙

## 프로젝트 구조

| 디렉토리 | 내용 |
|---------|------|
| `apps/vacuum_monitor/` | 진공 모니터 PyQt5 앱 |
| `apps/rheed_monitor/` | RHEED 모니터 HIKROBOT GigE 앱 |
| `src/` | ML 학습/평가 프레임워크 |
| `configs/exp/` | 실험 하이퍼파라미터 JSON |
| `experiments/` | 스윕 스크립트 및 run 기록 |
| `rainfilter_sim/` | 빗물 필터 시뮬레이션 |
| `tight binding/` | 타이트 바인딩 계산 코드 |

## 핵심 원칙

- 연구가 최우선 목표이므로 코딩 오버헤드를 최소화한다.
- 작은 타겟 변경을 선호하고 광범위한 리팩토링은 피한다.
- 대용량 데이터셋 폴더는 명시적 승인 없이 절대 건드리지 않는다.

## 실행 환경

- 코드는 **원격 PC 터미널**에서 실행한다 (로컬 머신 아님).
- `conda activate` 대신 직접 인터프리터 경로를 사용한다.
- 기본 Python 경로: `D:\conda_envs\torch\python.exe`

## 테스트 정책

코드 변경 후 프로젝트 성격에 맞는 검증을 실행한다:

- **모니터링/앱/스크립트**: 10초 실행으로 스모크 테스트
- **ML/추론**: 단일 샘플 추론으로 동작 확인

케이스마다 더 적합한 방법을 선택하고 어떤 것을 사용했는지 보고한다.

## 변경 전 사전 알림 필요 항목

아래에 해당하면 **진행 전에 반드시 알린다**:

- 코드 구조 대규모 변경
- 새 파일 생성
- 파일 삭제
- `json` 또는 `yaml` 설정 파일 수정
- 대용량 데이터셋 폴더 접근

## 작업 후 보고 항목

- 변경된 내용
- 실행한 테스트
- 주요 리스크 또는 후속 조치

## 배포 제약

- 관련 시 무설치(no-install), 오프라인 배포를 지원한다.
- 패키징 도구 선택 시 실험실 PC 환경 제약을 고려한다.
